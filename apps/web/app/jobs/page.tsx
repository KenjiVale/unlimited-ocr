"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {api,statusLabel} from "../../lib/api";import {Nav} from "../../components/nav";
export default function Jobs(){const [jobs,setJobs]=useState<any[]>([]);const [status,setStatus]=useState("");useEffect(()=>{api(`/api/ocr/jobs?limit=30${status?`&status=${status}`:""}`).then(x=>setJobs(x.items)).catch(()=>setJobs([]))},[status]);return <main><Nav/><h1>Jobs</h1><label>Status <select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All</option><option>QUEUED</option><option>PROCESSING</option><option>COMPLETED</option><option>FAILED</option></select></label><ul>{jobs.map(j=><li key={j.id}><Link href={`/jobs/${j.id}`}>{j.original_filename}</Link> · {j.file_type} · {statusLabel(j.status)} · {j.processed_pages}/{j.total_pages} · {new Date(j.created_at).toLocaleString()}</li>)}</ul>{!jobs.length&&<p>No jobs found.</p>}</main>}
